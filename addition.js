const addition = (num1, num2) => {
    let sum = num1 + num2;
    return sum;
}

const num1 = parseInt(prompt("Enter the first number to add: "));

const num2 = parseInt(prompt("Enter the second number to add: "));

console.log(addition(num1, num2));
